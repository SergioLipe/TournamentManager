# Copiar para .env e preencher. O .env está no .gitignore.

# development mostra os erros no browser; qualquer outro valor esconde-os.
APP_ENV=production

DB_HOST=localhost
DB_PORT=3306
DB_NAME=torneio_db
DB_USER=root
DB_PASS=
